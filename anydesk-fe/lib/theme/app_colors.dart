import 'package:flutter/material.dart';

class AppColors {
  // Brand & Accent Colors
  static const Color primary = Color(0xFF0058BC);
  static const Color primaryContainer = Color(0xFF0070EB);
  static const Color primaryAccent = Color(0xFFFFFFFF); // Pure White for Monochrome Dark Mode

  // Background Colors
  static const Color darkBackground = Color(0xFF0D0D0D);
  static const Color lightBackground = Color(0xFFFAF9FE);

  // Surface Colors (Dark Theme Default)
  static const Color darkSurface = Color(0xFF1C1C1E);
  static const Color darkSurfaceSecondary = Color(0xFF2C2C2E);
  static const Color darkSurfaceContainer = Color(0xFF141416);

  // Surface Colors (Light Theme)
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightSurfaceSecondary = Color(0xFFF2F2F7);

  // Text / Content Colors
  static const Color textPrimaryDark = Colors.white;
  static const Color textSecondaryDark = Color(0xFF9E9E9E);
  static const Color textPrimaryLight = Color(0xFF1A1B1F);
  static const Color textSecondaryLight = Color(0xFF414755);

  // Status Colors
  static const Color success = Color(0xFF34C759);
  static const Color error = Color(0xFFFF3B30);
  static const Color warning = Color(0xFFFF9500);

  // Borders & Dividers
  static const Color borderDark = Color(0x1FFFFFFF);
  static const Color borderLight = Color(0x1F000000);
}
